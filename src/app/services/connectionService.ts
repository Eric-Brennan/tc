// ============================================================
// Backward-compat re-export — use './shared/connectionService' directly
// ============================================================
export {
  listConnections,
  getConnectionById,
  createConnection,
  updateConnection,
  deleteConnection,
} from './shared/connectionService';
